projectItem: {
    padding: 12,
    borderBottomWidth: 1,
    borderBottom: '1px solid #eee',
    cursor: 'pointer',
  },
  projectItemText: {
    fontSize: 16,
  },
  noProjectsText: {
    padding: 16,
    textAlign: 'center',
    color: '#666',
  },
};

// Render the app
ReactDOM.render(<App />, document.getElementById('root'));
